// The Simulator UI is deliberately single-tenant (see
// docs/SIMULATOR_UI_DESIGN_PROMPT.md, "What NOT to include") -- each SE runs
// their own deployed instance, so there is exactly one tenant to operate on.
export const TENANT_SLUG = "company21912-poc";
